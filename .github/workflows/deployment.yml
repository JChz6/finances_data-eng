name: CI/CD Pipeline

on:
  push:
    branches:
      - main 
  pull_request:
    branches:
      - main

jobs:

#  test:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Check out code
#        uses: actions/checkout@v3
#
#      - name: Set up Python
#        uses: actions/setup-python@v4
#        with:
#          python-version: "3.12"
#
#      - name: Install dependencies
#        run: |
#          python -m venv env
#          source env/bin/activate
#          pip install --upgrade pip
#          pip install -r requirements.txt
#
#      - name: Lint and Format Check
#        run: |
#          source env/bin/activate
#          pip install flake8 black
#          black --check .  # Verifica el formateo
#          flake8 .         # Verifica errores de estilo y sintaxis


          

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt


      - name: Authenticate with GCP
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.CREDS_GCP }}

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v1
        with:
          service: ${{ vars.CLOUD_RUN_SERVICE }} # Cambia por el nombre de tu servicio en Cloud Run
          region: ${{ vars.CLOUD_RUN_REGION }}  # Cambia por tu región
          source: .

#gcloud run deploy function-3 --source . --region us-east4 --platform managed --allow-unauthenticated